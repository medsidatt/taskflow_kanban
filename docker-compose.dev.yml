networks:
  taskflow-network:
    driver: bridge

volumes:
  postgres_data:

services:

  postgres:
    image: postgres:16-alpine
    container_name: taskflow-postgres-dev
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - taskflow-network
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # In development, backend runs locally (not in container) for hot reload
  # If you want to run it in container, uncomment below and run: mvn spring-boot:run locally instead
  
  # backend:
  #   build:
  #     context: ./backend
  #     dockerfile: Dockerfile
  #   container_name: taskflow-backend-dev
  #   restart: unless-stopped
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   environment:
  #     SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/${POSTGRES_DB}
  #     SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER}
  #     SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD}
  #     SPRING_JPA_HIBERNATE_DDL_AUTO: update
  #     SPRING_JPA_SHOW_SQL: "true"
  #     SPRING_PROFILES_ACTIVE: dev
  #     JWT_SECRET: ${JWT_SECRET}
  #     JWT_EXPIRATION: ${JWT_EXPIRATION}
  #   ports:
  #     - "8080:8080"
  #   volumes:
  #     - ./backend:/app
  #   networks:
  #     - taskflow-network

  # In development, frontend runs locally (not in container) for hot reload
  # Run: npm start in frontend directory
